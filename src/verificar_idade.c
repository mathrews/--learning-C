#include "header.h"
#include <stdio.h>

void verificar_idade(int idade) {
    if (idade >= 18)
    {
        printf("Você já pode ser preso.");
    } else 
    {
        printf("Tu é um pivete. Klaboca.");
    }
}